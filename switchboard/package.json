{
  "name": "tickx-switchboard-price-integrity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "tsx src/server.ts",
    "deploy:devnet": "tsx src/deploy.ts",
    "simulate": "tsx src/simulate.ts",
    "snapshot:devnet": "tsx src/snapshot.ts",
    "settlement:devnet": "tsx src/settlement.ts",
    "settlement:deploy:devnet": "tsx src/settlement_deploy.ts",
    "settlement:prepare:commit:devnet": "tsx src/settlement_prepare_commit.ts",
    "settlement:commit:devnet": "tsx src/settlement_commit.ts"
  },
  "dependencies": {
    "@switchboard-xyz/common": "4.1.0",
    "@switchboard-xyz/on-demand": "2.14.5",
    "dotenv": "^16.6.1",
    "tsx": "^4.20.6"
  }
}
