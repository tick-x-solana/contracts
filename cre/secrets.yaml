# ==========================================================================
# CRE SECRETS TEMPLATE
# Copy to secrets.yaml and fill in values (never commit secrets.yaml to git!)
# ==========================================================================

local-simulation:
  secrets:
    APP_API_KEY: "mock-api-key-for-local-testing"
    APP_API_BASE_URL: "http://localhost:3000/api/v1"
    PRICE_INTEGRITY_ADDRESS: "0x0000000000000000000000000000000000000000"
    REPORTER_PRIVATE_KEY: "0x..."

staging:
  secrets:
    APP_API_KEY: "${APP_API_KEY}"
    APP_API_BASE_URL: "${APP_API_BASE_URL}"
    PRICE_INTEGRITY_ADDRESS: "${PRICE_INTEGRITY_ADDRESS}"
    REPORTER_PRIVATE_KEY: "${REPORTER_PRIVATE_KEY}"

production:
  secrets:
    APP_API_KEY: "${APP_API_KEY}"
    APP_API_BASE_URL: "${APP_API_BASE_URL}"
    PRICE_INTEGRITY_ADDRESS: "${PRICE_INTEGRITY_ADDRESS}"
    REPORTER_PRIVATE_KEY: "${REPORTER_PRIVATE_KEY}"
